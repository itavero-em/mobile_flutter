import 'package:flutter/material.dart';

//https://www.youtube.com/watch?v=c4dsvckUA1Q
//https://undraw.co/illustrations
class OnBoardingScreen extends StatelessWidget {
  const OnBoardingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: const [
          // Text("Willkommen"),
          // SvgPicture.asset("assets/undraw_real_time_sync_re_nky7.svg")
          //Image.asset("assets/undraw_real_time_sync_re_nky7.svg", height: 300,)
        ],
      ),
    );
  }
}
